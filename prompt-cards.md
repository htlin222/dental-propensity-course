# 學員提示詞卡｜牙科自費傾向模型

**用法**：把 `dental_patients.csv` 上傳給 AI，一次貼一段，看完結果再貼下一段。
**全程在同一個對話裡做**，不要開新視窗。

**開始前**
- 上傳：輸入框旁邊的迴紋針或 ＋
- AI 會吐一大堆，**程式碼可以略過，只看表格和圖**
- 它反問你的時候：回 `不用，請照我剛剛說的做就好`

---

## 1 認識這組資料

```
我上傳了一個牙科病人資料檔 dental_patients.csv。
我是初學者，請用 pandas 幫我：
1. 讀進來，告訴我有幾列幾欄、前 5 列長什麼樣
2. 每一欄有沒有缺失值
3. 數值欄位的最大最小值
4. 你覺得這份資料哪裡有問題？
請附上程式碼和白話解釋。
```

🔎 看一下 `age` 的最大值。合理嗎？

```
請刪掉完全重複的列、把 age 等於 999 的當成缺失用中位數補、
perio_depth 缺失也用中位數補。清理完印出現在有幾列。
```

✅ 應該是 **1,200 列**。

---

## 2 現況是多少

```
selfpay_next90 是我要預測的目標：1 = 未來 90 天有做自費，0 = 沒有。
請告訴我有多少人是 1、佔比多少，並畫一張長條圖（標籤用英文）。
```

🔎 **記住這個數字，它是基準線。**

---

## 3 誰比較會做自費

```
請算「不同族群的自費成交率」，每組顯示成交率和人數：
doctor、has_line、past_selfpay、income_area、sex、blood_type
再把 missing_teeth 分成 0顆 / 1-2顆 / 3-4顆 / 5顆以上，也算成交率。
最後挑差距最大的三個畫長條圖（標籤用英文）。
```

🔎 哪幾組差最多？哪幾組其實差不多？人數少的那組可信嗎？

---

## 4 做一個評分模型

```
請用 scikit-learn 訓練一個預測模型：
1. 讀入 dental_patients.csv，刪重複列，age 的 999 當缺失補中位數，perio_depth 補中位數
2. 特徵用：age, missing_teeth, perio_depth, no_show_rate, visits_12m,
   past_selfpay, has_line, income_area, doctor, sex, blood_type, days_since_visit
3. 類別欄位轉成 one-hot，數值欄位標準化，訓練 logistic regression
4. 印出 AUC，以及模型學到的各變數係數，由大到小排序
```

🔎 模型排出來的重要變數，跟你在第 3 步看到的一樣嗎？

```
幫我寫一個小函式，輸入一個病人的條件就得到成交機率。用這兩個例子跑：
病人A：58歲、缺4顆、牙周4.5、爽約0.05、回診3次、做過自費、有加LINE、high所得、C醫師、女、O型、90天前回診
病人B：28歲、缺0顆、牙周4.5、爽約0.35、回診3次、沒做過自費、沒加LINE、low所得、B醫師、女、O型、90天前回診
```

---

## 5 驗證（這裡才拿第二個檔案）

上傳 `dental_patients_new.csv`，然後：

```
我另外上傳了 dental_patients_new.csv，是完全不同的 1,200 位病人，
模型在訓練時從來沒看過他們。
請用剛剛訓練好的模型，對這批新病人打分數，然後告訴我：
1. 這批人的實際成交率是多少（基準線）
2. 模型的 AUC
3. 把他們依分數由高到低排序，前 10% / 前 20% / 前 30% 的實際成交率各是多少
4. 再分成五等分，每一等分的成交率畫成長條圖（標籤用英文）
```

🔎 前 20% 的成交率，是基準線的幾倍？

---

## 帶回去的三件事

1. **先算基準線** —— 不知道現在多少，就不知道有沒有進步
2. **描述性統計先做** —— 模型的結果應該跟你看到的一致
3. **一定要用沒看過的資料驗證** —— 用訓練資料評估自己，數字一定好看，而且沒有意義

---

## 卡住的時候

| 狀況 | 貼這句 |
|---|---|
| 圖的中文變方塊 | `請把圖表所有文字標籤改成英文` |
| 看不懂輸出 | `請用國中生能懂的方式解釋上面這張表` |
| AI 一次做太多 | `請只做我這一步要求的事，不要先做後面的步驟` |
| 程式報錯 | 貼整段錯誤訊息 + `請修正並解釋錯在哪` |
