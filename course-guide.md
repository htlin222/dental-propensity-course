# 牙科自費傾向模型｜60 分鐘實作課

學員不寫程式。複製提示詞給 AI，AI 跑程式，全班看結果。

**敘事是一條直線**：

> 有一組資料 → 提出一個問題 → 先做描述性統計 → 提出解決方法 → 驗證它可行

| 步驟 | 內容 | 分鐘 |
|---|---|---|
| 1 | 認識這組資料 | 10 |
| 2 | 提出問題，算出現況 | 8 |
| 3 | 描述性統計：誰比較會做自費 | 15 |
| 4 | 解決方法：做一個評分模型 | 15 |
| 5 | 驗證：拿全新一批病人來測 | 10 |
| — | 收尾 | 2 |

**課前**：發給每位學員兩個檔案

- `dental_patients.csv` — 1,200 位病人，用來建模
- `dental_patients_new.csv` — 另外 1,200 位全新病人，第 5 步才發，前面不要給

> 兩份都是電腦生成的模擬資料。回去之後不要把真實病歷直接上傳給 AI。

---

# STEP 1｜認識這組資料（10 分）

```
我上傳了一個牙科病人資料檔 dental_patients.csv。
我是初學者，請用 pandas 幫我：
1. 讀進來，告訴我有幾列幾欄、前 5 列長什麼樣
2. 每一欄有沒有缺失值
3. 數值欄位的最大最小值
4. 你覺得這份資料哪裡有問題？
請附上程式碼和白話解釋。
```

**結果**：1,206 列 × 15 欄。`perio_depth` 缺 55 筆。`age` 最大值是 **999**。

欄位的意思：

| 欄位 | 意思 |
|---|---|
| `selfpay_next90` | **要預測的目標**：未來 90 天有沒有做自費療程 |
| `missing_teeth` | 缺牙數 |
| `perio_depth` | 牙周囊袋深度 mm |
| `has_line` | 有沒有加診所 LINE |
| `past_selfpay` | 以前做過自費沒 |
| `no_show_rate` | 爽約率 |
| `doctor` | 主治醫師 A–D |
| `income_area` | 居住區所得 |
| 其他 | 年齡、性別、血型、回診次數、距上次回診天數 |

接著清理：

```
請刪掉完全重複的列、把 age 等於 999 的當成缺失用中位數補、
perio_depth 缺失也用中位數補。清理完印出現在有幾列。
```

**結果**：1,206 → **1,200 列**。

> 💬 **講師**：AI 自己就抓到 999 了。你的工作不是寫程式，是看到 999 會覺得怪。

---

# STEP 2｜提出問題，算出現況（8 分）

**問題**：

> 診所每個月只有 100 個諮詢名額，但回診病人有 1,200 個。
> **該優先找誰談自費？**

要回答之前，先知道現在做得怎麼樣：

```
selfpay_next90 是我要預測的目標：1 = 未來 90 天有做自費，0 = 沒有。
請告訴我有多少人是 1、佔比多少，並畫一張長條圖（標籤用英文）。
```

**結果**：**25.8%**。

> 💬 **講師**：現在隨便抓 100 個病人來談，大概成交 26 個。
> 這是基準線。等一下做出來的東西如果贏不過 26%，就沒有價值。

---

# STEP 3｜描述性統計：誰比較會做自費（15 分）

還沒有模型，先單純看數字。

```
請算「不同族群的自費成交率」，每組顯示成交率和人數：
doctor、has_line、past_selfpay、income_area、sex、blood_type
再把 missing_teeth 分成 0顆 / 1-2顆 / 3-4顆 / 5顆以上，也算成交率。
最後挑差距最大的三個畫長條圖（標籤用英文）。
```

**結果**：

| 分組 | 成交率 | 人數 |
|---|---|---|
| 缺牙 0 顆 | 13.7% | 277 |
| 缺牙 1–2 顆 | 26.4% | 671 |
| 缺牙 3–4 顆 | 36.9% | 214 |
| 缺牙 5 顆以上 | 42.1% | 38 |
| 有加 LINE / 沒加 | 30.5% / 17.9% | 754 / 446 |
| 做過自費 / 沒做過 | 37.5% / 21.5% | 325 / 875 |
| C 醫師 / B 醫師 | 43.0% / 19.3% | 263 / 295 |
| 所得區 high / low | 35.3% / 19.9% | 252 / 357 |
| 性別、血型 | 各組差距都在 6 個百分點以內 | — |

**三個可以直接講的觀察**：

1. **缺牙數越多，成交率越高**，而且一路上升，沒有例外
2. **加 LINE 和做過自費的人明顯較高** —— 這兩個是行為，不是臨床狀況
3. **醫師之間差兩倍以上**，這不是病人的差異

> 💬 **講師**：注意缺牙 5 顆以上那組只有 38 人。方向可信，數字別當真。
> 另外，性別和血型看起來有一點差，但差距小、而且沒有道理，先放著。

---

# STEP 4｜解決方法：做一個評分模型（15 分）

描述性統計只能一次看一個欄位。實際上一個病人是**同時**有缺牙、有加 LINE、掛 C 醫師的。
要把這些一起考慮，就需要模型。

### 先給名詞小抄（20 秒，不細講）

| 詞 | 一句話 |
|---|---|
| logistic regression | 一種算法，專門算「會/不會」的機率 |
| 訓練 / 測試 | 一部分拿去學，一部分藏起來當考卷 |
| 係數 | 模型自己算出來的「加幾分」 |
| AUC | 排序能力。0.5 = 丟銅板，1.0 = 完美 |

### 訓練

```
請用 scikit-learn 訓練一個預測模型：
1. 讀入 dental_patients.csv，刪重複列，age 的 999 當缺失補中位數，perio_depth 補中位數
2. 特徵用：age, missing_teeth, perio_depth, no_show_rate, visits_12m,
   past_selfpay, has_line, income_area, doctor, sex, blood_type, days_since_visit
3. 類別欄位轉成 one-hot，數值欄位標準化，訓練 logistic regression
4. 印出 AUC，以及模型學到的各變數係數，由大到小排序
```

**結果**：模型學到的前幾名 ——

| 變數 | 係數 |
|---|---|
| `income_area_low` | −0.94 |
| `doctor_C` | +0.92 |
| `income_area_mid` | −0.55 |
| `missing_teeth` | +0.42 |
| `has_line` | +0.37 |
| `past_selfpay` | +0.36 |

> 💬 **講師**：跟 STEP 3 用眼睛看到的一樣。
> **模型沒有發現新東西，它做的是把「加幾分」算出來，而不是我們用猜的。**

### 用它預測一個病人

```
幫我寫一個小函式，輸入一個病人的條件就得到成交機率。用這兩個例子跑：
病人A：58歲、缺4顆、牙周4.5、爽約0.05、回診3次、做過自費、有加LINE、high所得、C醫師、女、O型、90天前回診
病人B：28歲、缺0顆、牙周4.5、爽約0.35、回診3次、沒做過自費、沒加LINE、low所得、B醫師、女、O型、90天前回診
```

**結果**：病人 A **91%**，病人 B **3%**。

> 💬 **講師**：這就是我們的解決方法 —— 丟一個病人進去，它給一個機率。
> 但**現在還不能相信它**，因為它是用這 1,200 個人學出來的，
> 拿同一批人來考它，等於讓學生看著答案考試。

---

# STEP 5｜驗證：拿全新一批病人來測（10 分）

**這一步才發第二個檔案。**

```
我另外上傳了 dental_patients_new.csv，是完全不同的 1,200 位病人，
模型在訓練時從來沒看過他們。
請用剛剛訓練好的模型，對這批新病人打分數，然後告訴我：
1. 這批人的實際成交率是多少（基準線）
2. 模型的 AUC
3. 把他們依分數由高到低排序，前 10% / 前 20% / 前 30% 的實際成交率各是多少
4. 再分成五等分，每一等分的成交率畫成長條圖（標籤用英文）
```

**結果**：新病人的基準線是 **24.3%**，模型 **AUC 0.724**。

| 依分數排序 | 實際成交率 | 相對基準線 |
|---|---|---|
| **前 100 人（= 諮詢名額數）** | **66%** | **2.7 倍** |
| 前 10%（120 人） | 63.3% | 2.6 倍 |
| 前 20%（240 人） | 50.4% | 2.1 倍 |
| 前 30% | 43.3% | 1.8 倍 |
| 前 50% | 35.8% | 1.5 倍 |

五等分（分數由高到低）：**50.4% → 27.9% → 22.5% → 10.4% → 10.4%**

> 💬 **講師**（本課的結論）：
> 「這批人模型完全沒看過。它挑出來的前 100 個人，實際成交率 66%，
> 是隨便挑的 2.7 倍。**這就是驗證通過的意思。**
>
> 回到最初的問題：一個月 100 個諮詢名額該給誰？
> 答案是**照分數由高到低排前 100 個**，成交從 24 件變成 66 件。」

---

# 收尾（2 分）

今天走了一條完整的路：

| 步驟 | 我們做了什麼 | 得到什麼 |
|---|---|---|
| 1 | 認識並清理資料 | 1,200 筆乾淨資料 |
| 2 | 提出問題、算現況 | 基準線 25.8% |
| 3 | 描述性統計 | 缺牙、LINE、醫師是關鍵 |
| 4 | 建立評分模型 | 輸入病人 → 得到機率 |
| 5 | 用全新病人驗證 | 前 100 人命中 66%，2.7 倍 |

**要記住的三件事**：

1. **先算基準線。** 不知道現在多少，就不知道有沒有進步。
2. **描述性統計先做。** 模型的結果應該跟你看到的一致，不一致就要查。
3. **一定要用沒看過的資料驗證。** 用訓練資料評估自己的模型，數字一定好看，而且沒有意義。

---

## 應急包

| 狀況 | 處理 |
|---|---|
| 不知道怎麼上傳檔案 | 輸入框旁邊的迴紋針或 ＋ |
| AI 吐一大堆看不懂 | 程式碼可以略過，只看表格和圖 |
| 圖的中文變方塊 | 貼「請把圖表所有文字標籤改成英文」 |
| AI 反問要不要做別的 | 貼「不用，請照我剛剛說的做就好」 |
| 程式報錯 | 把錯誤訊息整段貼上 + 「請修正並解釋錯在哪」 |
| 找不到 sklearn | 貼「請先安裝 scikit-learn 再繼續」 |
| 數字跟手冊差 1–2 個百分點 | 正常，前處理寫法不同所致 |
| STEP 5 說找不到模型 | 提醒學員 STEP 4 和 STEP 5 要在同一個對話裡做 |
